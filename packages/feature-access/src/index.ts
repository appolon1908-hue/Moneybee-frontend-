export const bootstrapCapabilities = Object.freeze({
  'credit.live_pull': false,
  'lenders.live_submission': false,
  'esign.live_send': false,
  'funding.live_confirmation': false,
  payments: false,
  payouts: false,
} as const)
